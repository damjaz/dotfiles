#!/bin/bash

case $1 in
  convert)
    input="${2:-screencast.avi}"
    ffmpeg -i "$input" -vcodec libx264 -sameq "${input%.*}_x264.mp4"
    ;;
  *)
    output="${2:-screencast.avi}"
    frate="${1:-10}"
    ffmpeg -f x11grab -r $frate -s 1280x800 -i :0.0 -vcodec huffyuv "$output"
    ;;
esac

